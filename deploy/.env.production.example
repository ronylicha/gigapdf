# =============================================================================
# Application Configuration - PRODUCTION
# =============================================================================
APP_ENV=production
APP_DEBUG=false
APP_SECRET_KEY=CHANGE_ME_IN_PRODUCTION_MIN_32_CHARS_SECURE_KEY
APP_HOST=0.0.0.0
# APP_PORT / PYTHON_BACKEND_URL / NEXTJS_INTERNAL_URL must NOT be set here:
# they are per blue/green color, imposed by the systemd units written by
# deploy/server-deploy.sh (which neutralizes them if present — systemd
# EnvironmentFile= would otherwise override the per-color Environment= lines).
APP_WORKERS=4

# =============================================================================
# Database Configuration (Private Network)
# =============================================================================
DATABASE_URL=postgresql://rlicha:YOUR_PASSWORD_HERE
DATABASE_POOL_SIZE=20

# =============================================================================
# Redis Configuration (Private Network)
# =============================================================================
REDIS_URL=redis://172.16.8.4:6379/0

# =============================================================================
# Authentication (External JWT)
# =============================================================================
AUTH_JWT_PUBLIC_KEY=CONFIGURE_YOUR_JWT_PUBLIC_KEY
AUTH_JWT_ALGORITHM=RS256
AUTH_JWT_ISSUER=https://giga-pdf.com
AUTH_JWT_AUDIENCE=giga-pdf

# =============================================================================
# Storage Configuration
# =============================================================================
STORAGE_PATH=/var/lib/gigapdf/documents
STORAGE_MAX_SIZE_GB=100

# =============================================================================
# Semantic Search (#85)
# =============================================================================
# On-disk cache for the fastembed embedding model (384-d multilingual
# paraphrase-multilingual-MiniLM-L12-v2, ~470 MB). Downloaded once on first
# use, then reused offline. Created by deploy/setup-server.sh and owned by the
# gigapdf service user. Requires pgvector on the PostgreSQL server.
FASTEMBED_CACHE_DIR=/var/lib/gigapdf/fastembed-cache

# OCR engine (host-side, gigapdf-ocr-rten). The gigapdf-ocr.service binary
# (/opt/gigapdf/bin/ocr_serve) loads the .rten models once and listens on
# OCR_BIND; the Next.js web app reaches it at OCR_SERVICE_URL. Models + binary are
# fetched from the gigapdf-lib "ocr-rten-v1" release asset by deploy/redeploy.sh.
OCR_MODELS_DIR=/var/lib/gigapdf/rten-models/models
OCR_BIND=127.0.0.1:8077
OCR_SERVICE_URL=http://127.0.0.1:8077

# =============================================================================
# Celery Configuration
# =============================================================================
CELERY_BROKER_URL=redis://172.16.8.4:6379/1
CELERY_RESULT_BACKEND=db+postgresql://rlicha:YOUR_PASSWORD_HERE
JOB_TIMEOUT_SECONDS=3600
ASYNC_THRESHOLD_MB=10

# =============================================================================
# Limits
# =============================================================================
MAX_UPLOAD_SIZE_MB=100
MAX_PAGES_PER_DOCUMENT=5000
PREVIEW_MAX_DPI=600
HISTORY_MAX_STATES=100

# =============================================================================
# WebSocket Configuration
# =============================================================================
SOCKETIO_MESSAGE_QUEUE=redis://172.16.8.4:6379/2

# =============================================================================
# S3 Configuration (Scaleway)
# =============================================================================
S3_ACCESS_KEY_ID=YOUR_S3_ACCESS_KEY
S3_SECRET_ACCESS_KEY=YOUR_S3_SECRET_KEY
S3_BUCKET_NAME=gigapdf
S3_ENDPOINT=https://s3.fr-par.scw.cloud
S3_REGION=fr-par

# =============================================================================
# Email Configuration (SMTP)
# =============================================================================
MAIL_SERVER=mail.giga-pdf.com
MAIL_PORT=465
MAIL_USERNAME=nepasrepondre@giga-pdf.com
MAIL_PASSWORD=YOUR_PASSWORD_HERE
MAIL_FROM_EMAIL=nepasrepondre@giga-pdf.com
MAIL_FROM_NAME=GigaPDF
MAIL_USE_SSL=true
MAIL_TIMEOUT=30

# =============================================================================
# Frontend URLs
# =============================================================================
FRONTEND_URL=https://giga-pdf.com

# Next.js Public Variables
NEXT_PUBLIC_API_URL=https://giga-pdf.com/api
NEXT_PUBLIC_WS_URL=wss://giga-pdf.com
NEXT_PUBLIC_APP_URL=https://giga-pdf.com
NEXT_PUBLIC_ADMIN_URL=https://giga-pdf.com/admin

# =============================================================================
# Stripe Configuration
# =============================================================================
STRIPE_PUBLIC=YOUR_STRIPE_PUBLIC_KEY
STRIPE_SECRET=YOUR_STRIPE_SECRET_KEY
STRIPE_WEBHOOK=

# =============================================================================
# Scaleway API Configuration (for billing/infrastructure monitoring)
# Get these from: https://console.scaleway.com/iam/api-keys
# =============================================================================
SCW_ACCESS_KEY=YOUR_SCW_ACCESS_KEY
SCW_SECRET_KEY=YOUR_SCW_SECRET_KEY
SCW_DEFAULT_ORGANIZATION_ID=YOUR_SCW_ORG_ID
SCW_DEFAULT_PROJECT_ID=YOUR_SCW_PROJECT_ID
SCW_DEFAULT_REGION=fr-par
SCW_DEFAULT_ZONE=fr-par-1

# ─── Deployment targets (used by deploy/*.sh scripts) ───
# REQUIRED for running the deploy scripts.
DEPLOY_HOST=your.vps.example.com
GIGAPDF_VPS_HOST=your.vps.example.com
GIGAPDF_VPS_PATH=/opt/gigapdf
